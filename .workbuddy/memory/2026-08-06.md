# 2026-08-06

## @rulsynor/core 开源发布前优化

- 深入阅读 README、源码与测试，发现 README 旗舰示例参数顺序与规则格式错误、npx 命令不可用、规则数量不符（29 vs 32）、两条规则静默失效、运算符缺失、Playground 输出与示例不一致等问题。
- 新增 `toCompiledRules()` 作为官方入口；补齐 `matches`/`neq`/`not_exists`/`length_*` 等运算符；修复字符串误用 `gt` 的规则；新增 3 条安全规则使总数达 32。
- 修正 `provenance.ts` 版本与仓库地址；让 `copy-rules.mjs` 清空 `dist/rules/` 避免旧文件残留；为 `package.json` 增加 `@rulsynor/core` bin 入口。
- 重写 `README.md` 与 `README.zh-CN.md`，使示例代码、命令、输出与实际完全一致；Playground 现在输出真实替代建议。
- 输出优化报告 `OPTIMIZATION_REPORT.md` 与 `overview.md`。
- 验证：build 通过，47 个测试全部通过，Playground 输出与 README 一致。
