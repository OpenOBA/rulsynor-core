# 2026-08-06

## @rulsynor/core 开源发布前优化

- 深入阅读 README、源码与测试，发现 README 旗舰示例参数顺序与规则格式错误、npx 命令不可用、规则数量不符（29 vs 32）、两条规则静默失效、运算符缺失、Playground 输出与示例不一致等问题。
- 新增 `toCompiledRules()` 作为官方入口；补齐 `matches`/`neq`/`not_exists`/`length_*` 等运算符；修复字符串误用 `gt` 的规则；新增 3 条安全规则使总数达 32。
- 修正 `provenance.ts` 版本与仓库地址；让 `copy-rules.mjs` 清空 `dist/rules/` 避免旧文件残留；为 `package.json` 增加 `@rulsynor/core` bin 入口。
- 重写 `README.md` 与 `README.zh-CN.md`，使示例代码、命令、输出与实际完全一致；Playground 现在输出真实替代建议。
- 输出优化报告 `OPTIMIZATION_REPORT.md` 与 `overview.md`。
- 验证：build 通过，47 个测试全部通过，Playground 输出与 README 一致。

## @rulsynor/core 上线前全量审计（代码审查+安全+QA+开源合规）

- 对 rulsynor-core 执行上线前全量审计，覆盖代码质量、安全、QA 测试、开源合规四大维度，产出 `AUDIT-REPORT.md`。
- 验证当前状态：tsc --noEmit 0 错误 ✅，76 测试全绿 ✅，实际规则数 28（文档说 32/29/31 均不符）。
- 识别 7 项 P0 阻断性问题：
  - P0-01 正则注入+ReDoS（safe-expr.ts:152 等 3 处 new RegExp，无 ReDoS 防护）
  - P0-02 Runtime 完全绕过 Guard（runtime.ts:103 传入空规则数组）
  - P0-03 生产默认 VirtualClock（guard-state-manager.ts:35、evaluator-adapter.ts:145，时间静止导致 within/rate 失效）
  - P0-04 ESM 中使用 __dirname（rule-compiler.ts:126、op-sem-registry.ts:63，运行时 ReferenceError）
  - P0-05 Evaluator 评估副作用+破坏 first-match-wins（evaluator.ts:80-125，pre-check 写计数器且跳过排序）
  - P0-06 占位符合规声明法律风险（provenance.ts:44,50 CAC 备案号占位）
  - P0-07 SafeExpr 递归无深度限制（栈溢出 DoS）
- 识别 18 项 P1（版本号不一致、Clock 接口冲突、fail-open 默认、verifyEquivalence 空实现、.gitignore 错误排除 lockfile、缺 ESLint/CI/社区文件、大量 as any 违背确定性定位等）+ 10 项 P2。
- 每个问题附路径、行号、修复代码、验证命令。建议 Sprint 0 修复全部 P0（~25h）后再首次公开 tag。
