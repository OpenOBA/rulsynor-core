# 版本策略与生命周期（Versioning & Lifecycle）

> 本文档是 `@openoba/rulsynor-core` 软件版本全生命周期的唯一权威约定。
> 任何发布、升级、废弃决策必须遵循本规范；修改本文件走 PR + 维护者评审。

---

## 1. 版本号规范（Semantic Versioning）

遵循 [SemVer 2.0.0](https://semver.org/)，格式 `MAJOR.MINOR.PATCH`：

| 位 | 何时递增 | 例子 |
|----|---------|------|
| **MAJOR** | 不兼容的 API 变更（breaking change） | `0.1.0 → 1.0.0` |
| **MINOR** | 向后兼容的新功能 | `0.1.0 → 0.2.0` |
| **PATCH** | 向后兼容的 bug 修复 | `0.1.0 → 0.1.1` |

预发布标识（`-` 后接，按优先级递增）：`-alpha` → `-beta` → `-rc`。

> **`0.x.y` 特殊规则**：主版本为 0 时，API 不稳定——`0.1.0 → 0.2.0` 允许 breaking（视为 MINOR 级变更）。

---

## 2. 版本生命周期阶段

每个版本号在其生命周期内经历以下阶段：

| 阶段 | 含义 | API 稳定性 | 面向对象 | 准入/退出标准 |
|------|------|-----------|---------|--------------|
| **alpha** | 功能未完成，早期验证 | 无承诺，可随时 breaking | 内部 / 早期合作方 | 退出：功能完整 + DO v1.5 对齐 |
| **beta** | 功能基本完整，公开测试 | 低，变更需提前一个 MINOR 公告 | 公开测试者 | 退出：无 P0/P1 缺陷 |
| **rc** | 候选发布，仅修 bug | 冻结 | 待上线验证 | 退出：稳定期无回归 |
| **stable** | 稳定发布 | **SemVer 承诺** | 全体用户 | — |
| **deprecated** | 已废弃，仍可安装 | 不新增功能 | 存量用户 | 退出：进入 EOL |
| **EOL** | 终止维护 | 不再支持 | — | — |

---

## 3. 版本号提升规则

| 变更类型 | 版本动作 | 示例 |
|---------|---------|------|
| 新增导出/枚举/类型（非破坏） | MINOR | `0.1.0 → 0.2.0` |
| 修改现有 API 签名/行为 | MAJOR（0.x 下为 MINOR） | `0.1.0 → 0.2.0` |
| 仅修 bug | PATCH | `0.1.0 → 0.1.1` |
| 进入公开测试 | `-alpha → -beta` | `0.1.0-alpha → 0.1.0-beta` |

**判定原则**：无法确定是否 breaking 时，从严——宁可升 MAJOR 也不冒险。

---

## 4. 兼容性承诺（Compatibility Contract）

- **stable 版**：同一 MAJOR 内，MINOR/PATCH 升级 MUST 向后兼容（不破坏公共 API、不改决策语义）。
- **运行时**：`engines.node >= 20`（见 `package.json`）；升级 Node 最低版本视为 breaking（MAJOR）。
- **决策对象格式**：`preimage_version` 常量（如 `erdl-do-v1.5-hash-flat`）冻结后不可变，变更走 RFC 流程 + 破坏性版本。
- **确定性**：同一规则、同一输入的求值结果 MUST 跨版本稳定；任何改变求值结果的改动 MUST 记入 CHANGELOG 的 **Changed** 并升 MAJOR（0.x 下 MINOR）。

---

## 5. 废弃政策（Deprecation Policy）

1. **公告**：废弃某个 API，至少提前**一个 MINOR 版本**在 CHANGELOG `Deprecated` 段公告。
2. **保留**：`deprecated` 标记后，该 API 在**同一 MAJOR 内继续可用**（打 warning，不报错）。
3. **移除**：进入下一个 MAJOR 时移除（配合 `Removed` 段说明替代方案）。
4. **EOL**：某 MAJOR 的 EOL 至少提前 **3 个月**公告；EOL 后不再修 bug、不再发补丁。

---

## 6. 当前版本状态

| 版本 | 阶段 | 状态 |
|------|------|------|
| `0.1.0-alpha` | alpha | 当前开发中（引擎已对齐 Spec v2.0，DO 仍 v1.5 迁移中） |
| `1.0.0` | **已废弃（superseded）** | 2026-08-07 过早发布，已从版本线撤回，不继续维护 |

> 说明：`1.0.0` 是引擎未对齐 Spec v2.0 时的过早发布，现版本线已重启为 `0.1.0-alpha`。
> 已安装 `1.0.0` 的用户请迁移到 `0.1.0-alpha` 之后的新版本线（见 CHANGELOG 迁移说明）。
